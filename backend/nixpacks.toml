[phases.setup]
# Only install Tesseract as a system package.
# Nixpacks auto-detects Python from requirements.txt and handles pip itself.
nixPkgs = ["tesseract"]

[start]
cmd = "uvicorn app:app --host 0.0.0.0 --port $PORT"
